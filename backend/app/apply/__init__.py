"""Application agent — preparation (pure) + browser automation (optional)."""
from app.apply.prepare import ApplicationPlan, OpenQuestion, PlannedField, prepare_application

__all__ = ["ApplicationPlan", "OpenQuestion", "PlannedField", "prepare_application"]
